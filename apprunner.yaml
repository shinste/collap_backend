version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
      - pip install pipenv
      - pipenv install 
run:
  command: sh startup.sh
  network:
    port: 8000