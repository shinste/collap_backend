version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install -r requirements.txt
      - python3 manage.py collectstatic --noinput
      - pipenv install
run:
  runtime-version: 3.11
  command: pipenv run gunicorn collap.wsgi --log-file -
  network:
    port: 8000